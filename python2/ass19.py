def locks_and_keys(locks,keys):
	print "U hav %d locks!"%locks
	print "U hav %d keys!"%keys

locks_and_keys(20,30)
lock_count=50
key_count=55
locks_and_keys(lock_count,key_count)
locks_and_keys(10+20,15+9)
locks_and_keys(lock_count+10,key_count+9)
